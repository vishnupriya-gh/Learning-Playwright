JavaScript Definitions

This folder contains a Word-compatible document with concise definitions of:
- V8 Engine
- var / let / const
- Keyword, Identifier, Literal, Operator

Files:
- JavaScript_Definitions.doc — Word-compatible RTF content (open with MS Word).

If you want a true `.docx` instead, reply and I'll create one.